import PropTypes from 'prop-types';

const Event = ({event}) => {
    return (
        <div>
            
        </div>
    );
};

Event.propTypes = {
    event: PropTypes.object
};

export default Event;